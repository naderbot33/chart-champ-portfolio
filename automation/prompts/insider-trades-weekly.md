Run the ChartChamp weekly insider-trades dry run. Verify Chrome, the Codex extension, and TrendSpider sign-in, then confirm today is a U.S. equity session. Check the visible Sidekick allowance; if one prompt would leave fewer than 10 messages, skip and log `SIDEKICK_LIMIT`. Ask Sidekick once for notable insider activity filed during the prior seven calendar days.

Prioritize clusters and open-market purchases or sales; exclude routine grants, tax withholding, and automatic-plan transactions unless genuinely material. Verify every included item against the relevant SEC Form 4, including filer, relationship, transaction code, date, shares, price, and approximate value. Include no more than five items and explain that insider activity by itself is not predictive. Keep the complete Discord content at or below 1,900 characters.

Create exactly `{channelId, generatedAt, sourceTimestamp, content, dedupeKey, dryRun}` for channel `1209208579115057243`, with `dryRun: true`. Create validator context with Chrome state, `chrome.signedIn.trendspider`, `sidekick.messagesRemaining`, market date, `source.itemCount`, `source.form4sVerified: true`, and `source.sevenDayWindow: true`. Run `scripts/dispatch-discord-draft.mjs` for job `insider-trades-weekly`. Do not type into Discord, call a webhook directly, or change `dryRun`.
