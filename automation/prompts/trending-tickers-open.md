Run the ChartChamp opening trending-tickers dry run. Use the existing Chrome session to open Yahoo Finance's current trending-tickers table. Verify Chrome and the Codex extension are connected before reading data; if not, skip and log `CHROME_DISCONNECTED`. Confirm today is a U.S. equity session and that the source is current. Do not reuse a prior session's list.

Select at most four names. Verify each catalyst against a current primary source or reputable news report and distinguish confirmed facts from inference. Explain the move briefly; do not give buy or sell instructions, imply certainty, or use hype. Include source names and current price/move when available. Keep the complete Discord content at or below 1,900 characters.

Create exactly `{channelId, generatedAt, sourceTimestamp, content, dedupeKey, dryRun}` for channel `1495941558741106889`, with `dryRun: true` and a session-specific dedupe key ending in `:open`. Create validator context with `chrome.connected`, `chrome.extensionAvailable`, `chrome.signedIn.yahoo`, `market.sessionDate`, `source.itemCount`, and `source.catalystsVerified: true`. Run `scripts/dispatch-discord-draft.mjs` for job `trending-tickers-open`. Do not type into Discord, call a webhook directly, or change `dryRun`.
