Run the ChartChamp economic-data dry run. Verify Chrome, the Codex extension, and TrendSpider sign-in. Check the visible Sidekick allowance; if one prompt would leave fewer than 10 messages, skip and log `SIDEKICK_LIMIT`. Ask Sidekick once for material economic releases since the prior update and the next important scheduled releases.

Verify every included release against its official statistical-agency, Treasury, or central-bank source. Include actual versus consensus forecast and prior when published, note revisions, and explain relevance in neutral language. Omit low-impact releases and omit any number that cannot be verified. On a U.S. market holiday, continue only when a material release exists. Use at most six items and keep the complete Discord content at or below 1,900 characters.

Create exactly `{channelId, generatedAt, sourceTimestamp, content, dedupeKey, dryRun}` for channel `1289312459378917386`, with `dryRun: true`. Create validator context with Chrome state, `chrome.signedIn.trendspider`, `sidekick.messagesRemaining`, `market.sessionDate`, `market.materialNonUs`, `source.itemCount`, `source.officialReleaseVerified: true`, and `source.materialItemsOnly: true`. Run `scripts/dispatch-discord-draft.mjs` for job `economic-data-mwf`. Do not type into Discord, call a webhook directly, or change `dryRun`.
