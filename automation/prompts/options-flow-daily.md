Run the ChartChamp options-flow dry run. Verify Chrome, the Codex extension, and TrendSpider sign-in, then confirm today is a U.S. equity session. Check the visible Sidekick allowance; if one prompt would leave fewer than 10 messages, skip and log `SIDEKICK_LIMIT`. Ask Sidekick once for the current session's most significant options flow, including premium, strike, expiry, volume versus open interest, and whether the trade executed near bid or ask when available.

Include no more than three apparently bullish and three apparently bearish observations. Explicitly state that opening versus closing, spreads, stock hedges, and trader intent may be unknown, so directional labels are not conclusions or return predictions. Avoid personalized advice. Keep the complete Discord content at or below 1,900 characters and timestamp the source.

Create exactly `{channelId, generatedAt, sourceTimestamp, content, dedupeKey, dryRun}` for channel `1493981552005353616`, with `dryRun: true`. Create validator context with Chrome state, `chrome.signedIn.trendspider`, `sidekick.messagesRemaining`, market date, `source.bullishCount`, `source.bearishCount`, and `source.intentUncertaintyLabeled: true`. Run `scripts/dispatch-discord-draft.mjs` for job `options-flow-daily`. Do not type into Discord, call a webhook directly, or change `dryRun`.
