Run the ChartChamp closing market-scanners dry run. Verify Chrome, the Codex extension, and TrendSpider sign-in. Confirm today is a U.S. equity session. Use only saved or subscribed scanners and stop immediately if an unsaved-changes warning appears. Never edit or save a scanner definition.

Run Minervini VCP Detector every day. On Tuesday and Thursday also run Trend Template Perfect Score. On Monday, Wednesday, and Friday also run the saved CAN SLIM NYSE scanner. On Friday also run Quality Compounder. Capture the actual result set and timestamp from every scanner. Then check the visible Sidekick allowance; if one prompt would leave fewer than 10 messages, skip and log `SIDEKICK_LIMIT`. Use one Sidekick prompt to analyze only the actual results. Select at most five bullish standouts based on observable setup evidence, liquidity, trend, and nearby invalidation risk. Scanner matches are not predictions; do not give buy or sell instructions. Keep the Discord content at or below 1,900 characters.

Create exactly `{channelId, generatedAt, sourceTimestamp, content, dedupeKey, dryRun}` for channel `1495962636016160778`, with `dryRun: true` and a dedupe key ending in `:close`. Context must include Chrome state, `chrome.signedIn.trendspider`, `sidekick.messagesRemaining`, market date, `scanner.unsavedChangesWarning: false`, `scanner.savedOrSubscribedOnly: true`, the scanners actually run, `source.itemCount`, and `source.actualScannerResults: true`. Run `scripts/dispatch-discord-draft.mjs` for job `market-scanners-close`. Do not type into Discord, call a webhook directly, or change `dryRun`.
